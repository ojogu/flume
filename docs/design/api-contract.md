This document explains in detail (technical) the API contract for the B2B API

We are keeping it lean, and minimal. We have one resource /jobs  with standard REST operations on it.

API endpoints and schema
