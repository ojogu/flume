# Authentication

Flume uses Bearer Token authentication for all public API requests.

## Header
All requests must include the `Authorization` header:

```http
Authorization: Bearer <your_api_key>
```

Learn how to obtain an API key in the [API Keys](/docs/guides/api-keys) section.
